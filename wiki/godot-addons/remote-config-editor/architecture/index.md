# Architecture

**Status:** active

## Overview
How the addon is built: the headless `RemoteConfigService` core, the bottom-panel dock view, and the config seam (`remote_config_editor.config.json` + the path resolver) that makes it game-agnostic.

## Contents
- [Remote Config Editor](architecture:mql8pmvd-02fg-pqih2o)
