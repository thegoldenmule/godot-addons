# Architecture

**Status:** active

## Overview
How the Editor Tool Kit addon is built: the framework root and its primitives — the `EditorToolPlugin` composition root, the headless-testable `ToolService`, `ContentStore`, `EditorToolUi`, the cascaded palette/theme, `BridgeServer`, and the self-update subsystem.

## Contents
- [Editor Tool Kit](architecture:mql3ccsv-01q2-v81c5e)
