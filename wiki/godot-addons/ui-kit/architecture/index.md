# Architecture

**Status:** active

## Overview
How the UI Kit addon is built: the UI Kit package and its primitives — `UiRouter` (async stack-FSM), `UiState`, `UiScreenScaffold`, `UiReg` (control registration), `UiDriver` (semantic automation), and `RouterSceneState`.

## Contents
- [UI Kit](architecture:mql3cw6p-01r7-5abf1v)
