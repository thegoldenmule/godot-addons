# Godot
.godot/
*.import
export_presets.cfg

# OS
.DS_Store

# Self-update staging (only ever written into a consuming project's user://).
# editor_tool_kit is the package manager — it stages every addon's update here,
# so there is one staging dir regardless of how many addons are managed.
editor_tool_kit_update/
